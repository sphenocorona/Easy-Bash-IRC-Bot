echo "PRIVMSG $2 :$1: To use a module via PM, type /PRIVMSG $nick ~modulename [args] (where args represents arguments separated by spaces)"
echo "PRIVMSG $2 :To use a module in-channel, type ~modulename [args]"
